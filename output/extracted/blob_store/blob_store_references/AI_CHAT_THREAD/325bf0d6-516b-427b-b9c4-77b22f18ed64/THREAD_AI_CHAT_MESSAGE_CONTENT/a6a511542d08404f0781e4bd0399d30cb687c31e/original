{"filePath":"/components/ExecutiveMembersPage.tsx","snippetStartLine":597,"tagLine":598,"snippet":"<div className=\"flex items-center justify-center gap-3 mt-16\">\n                  {messages.map((_, index) => (\n                    <button\n                      key={index}\n                      onClick={() => setCurrentMessageIndex(index)}\n                      className=\"relative rounded-full transition-all overflow-hidden\"\n                      aria-label={`Go to message ${index + 1}`}\n                    >\n                      {index === currentMessageIndex ? (\n                        <>\n                          {/* Background dot */}\n                          <div className=\"w-16 h-1.5 bg-white/30 rounded-full\" />\n                          {/* Animated progress fill */}\n                          <motion.div\n                            className=\"absolute top-0 left-0 h-full bg-white rounded-full\"\n                            style={{ width: `${progress}%` }}\n                            transition={{ type: false }}\n                          />\n                        </>\n                      ) : (\n                        <div className=\"w-2.5 h-1.5 bg-white/40 hover:bg-white/60 rounded-full transition-colors\" />\n                      )}\n                    </button>\n                  ))}\n                </div>","tagName":"div"}